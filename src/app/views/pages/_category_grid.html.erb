      <div class="column grid_<%= grid %>">
        <h3><%= t(".#{category.i18n_key}", default: "#{category.name} Sessions") %></h3>
        <ul class="sessionsList">
          <%= render category.sessions.for_current_event.limit(5) %>
        </ul>
        <p><%= link_to 'See more...', category_path(category) %></p>
      </div>

